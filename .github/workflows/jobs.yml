# Challenge Author: None
# Automatically generated by Pwntest on 2023-02-26 12:13:50.156345

name: jobs

on:
  pull_request:
    branches: [ "master" ]
    paths:
      - None**

jobs:
  build_and_test:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Set up Python 3.10
        uses: actions/setup-python@v3
        with:
          python-version: '3.10'
      - name: Test with pytest
        run: |
          pip3 install pytest
          pytest None
          
        env:
          CONTAINER_HOST: localhost

