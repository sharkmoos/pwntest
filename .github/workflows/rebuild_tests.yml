# Created by:
# Automatically generated by Pwntest on

name: Auto-regenerate Tests

on: [ "push" ]

permissions:
  contents: write
  actions: write

jobs:
  create_tests:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}
      - name: Generate Tests
        uses: actions/setup-python@v3
        with:
          python-version: '3.10'
      - name: Install dependencies and Regenerate Actions
        run: |
          pip3 install ruamel.yaml # pwntest
          python3 pwntest/commandline/pwntest_action.py -r -m pwntest_manifest.yml
          # pwntest_action -r -m pwntest_manifest.yml
      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "Auto-regenerate tests"
          token: ${{ secrets.WORKFLOW_TOKEN }}
